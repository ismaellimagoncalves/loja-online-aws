name: CI/CD Pipeline

on:
  push:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout dos repositórios
        uses: actions/checkout@v3

      - name: Login Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and Push Docker image
        run: |
          sudo docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/loja-online:v2 .
          sudo docker push ${{ secrets.DOCKERHUB_USERNAME }}/loja-online:v2

  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy on EC2 via SSH
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            sudo docker stop loja || true
            sudo docker rm loja || true
            sudo docker pull ${{ secrets.DOCKERHUB_USERNAME }}/loja-online:v2
            sudo docker run -d -p 80:5000 --name loja ${{ secrets.DOCKERHUB_USERNAME }}/loja-online:v2
